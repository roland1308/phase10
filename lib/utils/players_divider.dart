import 'package:flutter/material.dart';

class PlayersDivider extends StatelessWidget {
  const PlayersDivider({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Divider();
  }
}
